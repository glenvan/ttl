# yaml-language-server: $schema=https://taskfile.dev/schema.json
# https://taskfile.dev

version: '3'

tasks:
  default:
    cmds:
      - task: test
    silent: true

  test:
    cmds:
      - go test ./...
    silent: true

  test-verbose:
    cmds:
      - go test -v ./...
    silent: true
